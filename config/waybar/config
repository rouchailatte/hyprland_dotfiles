{
    "layer": "top",
    "position": "top",
    "height": 30,
    "spacing": 0,
    "margin-top": 0,
    "margin-bottom": 0,
    "margin-left": 0,
    "margin-right": 0,

    "modules-left": ["hyprland/workspaces", "hyprland/window"],
    "modules-center": ["clock"],
    "modules-right": ["cpu", "memory", "disk", "temperature", "pulseaudio", "network", "battery", "custom/theme", "custom/font", "custom/kitty-theme", "tray"],

    "hyprland/workspaces": {
        "format": "{id}",
        "on-click": "activate",
        "format-icons": {
            "active": "█",
            "default": "▯"
        },
        "persistent-workspaces": {
            "*": 5
        }
    },

    "hyprland/window": {
        "format": "{}",
        "separate-outputs": true,
        "max-length": 50
    },

    "clock": {
        "format": "{:%H:%M}",
        "format-alt": "{:%Y-%m-%d %H:%M:%S}",
        "tooltip-format": "<tt>{calendar}</tt>",
        "calendar": {
            "mode": "month",
            "mode-mon-col": 3,
            "on-scroll": 1,
            "format": {
                "months": "<span color='#000000'><b>{}</b></span>",
                "weekdays": "<span color='#000000'><b>{}</b></span>",
                "today": "<span color='#000000'><b>{}</b></span>"
            }
        }
    },

    "battery": {
        "states": {
            "warning": 30,
            "critical": 15
        },
        "format": "{capacity}%",
        "format-charging": "{capacity}% ↑",
        "format-plugged": "{capacity}%",
        "format-alt": "{time}",
        "format-full": "100%",
        "max-length": 25
    },

    "network": {
        "format-wifi": "{essid} {signalStrength}%",
        "format-ethernet": "ETH",
        "tooltip-format": "{ifname} via {gwaddr}\n{ipaddr}/{cidr}",
        "format-linked": "{ifname} (No IP)",
        "format-disconnected": "NO NET",
        "format-alt": "{ifname}: {ipaddr}/{cidr}",
        "on-click": "nm-connection-editor"
    },

    "pulseaudio": {
        "format": "VOL {volume}%",
        "format-muted": "MUTE",
        "on-click": "pavucontrol",
        "max-length": 10
    },

    "tray": {
        "spacing": 10
    },

    "cpu": {
        "format": "CPU {usage}%",
        "tooltip": true,
        "interval": 2
    },

    "memory": {
        "format": "RAM {percentage}%",
        "tooltip-format": "{used:0.1f}GB / {total:0.1f}GB used",
        "interval": 5
    },

    "disk": {
        "format": "DISK {percentage_used}%",
        "path": "/",
        "tooltip-format": "{used} / {total} used ({percentage_used}%)",
        "interval": 30
    },

    "temperature": {
        "format": "TEMP {temperatureC}°C",
        "critical-threshold": 80,
        "format-critical": "TEMP {temperatureC}°C!",
        "interval": 5
    },

    "custom/theme": {
        "format": "THEME",
        "tooltip": true,
        "tooltip-format": "Click to change color theme",
        "on-click": "$HOME/.config/hypr/theme-switcher.sh"
    },

    "custom/font": {
        "format": "FONT",
        "tooltip": true,
        "tooltip-format": "Click to change system font",
        "on-click": "$HOME/.config/hypr/font-switcher.sh"
    },

    "custom/kitty-theme": {
        "format": "TERM",
        "tooltip": true,
        "tooltip-format": "Click to change terminal theme",
        "on-click": "$HOME/.config/hypr/kitty-theme-switcher.sh"
    }
}
